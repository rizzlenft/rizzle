# Copy to .env for local development. Cloudflare Pages reads these from the
# committed .env today; you can also set them in the Pages dashboard instead.
VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your_anon_public_key_here
